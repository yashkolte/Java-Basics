import java.util.Arrays;
import java.util.Scanner;

public class ArreyProblems {
    public static void main(String[] args) {
        // int num = 8;
        // int arrsize = 5;
        // int arr[] = new int[arrsize];
        // Scanner sc = new Scanner(System.in);
        // for(int i = 0; i < arrsize; i++) {
        //     System.out.print("Enter the " + i + " Numbers : ");
        //     arr[i] = sc.nextInt();
        // }

        // for(int i = 0; i < arrsize; i++){
        //     if (arr[i] == num) {
        //         System.out.println("The number " + num + " is at array's " + i + " position.");
        //     } else {
        //         System.out.println("The number " + num + " not exist in the arrey.");
        //     }
        // }

        //! Take an array of names as input from the user and print them on the screen.
        
        // Scanner sc = new Scanner(System.in);
        // System.out.print("How many user name you want to print : ");
        // int numOfUser = sc.nextInt();
        // String names[] = new String[numOfUser];
        // // Input for Names in Array
        // for(int i = 0; i < numOfUser; i++){
        //     System.out.print("Enter The User Name : ");
        //     names[i] = sc.next();
        // }
        // // Output 
        // System.out.println("The User Names are " + Arrays.toString(names));

        int arr[] = {100, 59, 35, 25, 10, 8, 5, 14};
        // int max = Integer.MIN_VALUE;
        // int min = Integer.MAX_VALUE;

        // for(int i = 0; i < arr.length; i++) {
        //     if (arr[i] < min) {
        //         min = arr[i];
        //     }
        //     if (arr[i] > max) {
        //         max = arr[i];
        //     }
        // }
        // System.out.println(min);
        // System.out.println(max);

        


    }
}
